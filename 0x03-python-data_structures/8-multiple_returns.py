#!/usr/bin/python3
def multiple_returns(sentence):
    if sentence == '':
        first = None
        my_tup = len(sentence), first
        return my_tubtup

    my_tup = len(sentence), sentence[0]
    return my_tup
